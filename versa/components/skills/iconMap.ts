import { Layout, Server, Database, Layers, Shield, Rocket, type LucideIcon } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  layout: Layout,
  server: Server,
  database: Database,
  layers: Layers,
  shield: Shield,
  rocket: Rocket
};
